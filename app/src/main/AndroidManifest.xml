<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.aku.hassannaqvi.psbitrial">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PRIVILEGED_PHONE_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />


    <application
        android:name=".core.MainApp"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:roundIcon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/PSBI">
        <activity
            android:name=".ui.sections.Section4Activity"
            android:theme="@style/AppThemeEnglish1" />
        <activity
            android:name=".ui.sections.Section5Activity"
            android:theme="@style/AppThemeEnglish1" />

        <activity
            android:name=".ui.sections.OnholdForm"
            android:theme="@style/AppThemeEnglish1" />
        <activity
            android:name=".ui.sections.Section3Activity"
            android:theme="@style/AppThemeEnglish1" />
        <activity
            android:name=".ui.sections.Section2Activity"
            android:theme="@style/AppThemeEnglish1" />

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".ui.sections.Section1Activity"
            android:theme="@style/AppThemeEnglish1" />
        <activity
            android:name=".ui.LoginActivity"
      >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.SyncActivity"
          />

        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".ui.sections.EndingActivity"

            android:theme="@style/AppThemeEnglish1">

        </activity>
    </application>

</manifest>